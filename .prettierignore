# Generated by the pipeline, not hand-edited. Reformatting it would fight
# build_site_content.py and break its "template matches site.json" test.
data/
out/

# Hand-written, and deliberately compact: many rules are one-liners that group
# a whole idea on a line. Prettier expands every one of them, which grew the
# file by half and destroyed that grouping. Delete this line if you would
# rather have the consistency.
src/app/globals.css

.next/
package-lock.json
